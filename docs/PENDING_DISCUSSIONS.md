# 논의 대기 목록 — 결정되면 지우고, 결정 근거는 해당 설계 문서로 옮긴다

> 시작 2026-08-05. "하기로 했는데 미룬 것"과 "제안됐는데 결정 안 난 것"을 여기 모은다.
> 완료·기각 시 줄을 지우되, 결정 이유가 남을 가치가 있으면 관련 문서에 옮겨 적는다.

## 2. 낮 2차 생성 실행 — 반려 당일 보충 (제안 상태, 미결정)

- 문제: 하루 1편 공개가 목표인데, 아침 반려의 대체 생성이 그날 밤이라 버퍼(대기 3)가
  얇을 땐 하루 공백이 날 수 있다.
- 제안: 낮(예 13:00)에 생성 실행 1회 추가 — scene_loop 은 대기 미달 채널만 생성하므로
  반려 없던 날은 몇 초 skip(비용 ~0), 반려 있던 채널만 재생성 → 오후 검수 → 19:00 슬롯.
- 결정 필요: 도입 여부 · 시각(운영자 검수 시간대 역산) · assignments.json 선언 방식.

## 3. GITHUB_TOKEN 시크릿 (커밋·배포 탭 100%)

- Fine-grained 토큰(두 레포 Contents:Read)을 Supabase 시크릿에 넣으면 커밋 목록·"몇 커밋
  뒤" 표시가 완성된다. 없어도 pull 매트릭스는 다수결 비교로 동작 중.

## 4. 맥4 브랜치 정리 (`chore/luna4-works-cards`, 원격 대비 28커밋 앞)

- 러너 한 대만 main 이 아니면 "6대 동일" 전제가 깨지고 치명 수정이 pull 로 안 들어온다
  (8/5 한글 키 픽스 때 실측 — 맥4 가 직접 병합으로 때움). 맥4 가 푸시/PR 정리 제안한 상태.

## 5. 반려분 보존 정리 스크립트 (`scripts/list_review_cleanup.py`, 미구현)

- 설계(STORAGE_DESIGN §6): 반려 후 14일 지난 Storage 사본 목록 출력 → 사람이 삭제.
  발행분 자동 정리는 이미 동작하므로 급하지 않음 — 반려 누적량을 보고 착수.

## 6. React 단일 파일 → Vite 빌드 전환 (트리거 합의됨, 시점 미정)

- 트리거(2026-08-05 합의): 화면 수정 주 1회+ 일상화 / 고급 상호작용 필요 / 다수 인원이
  프런트 수정 시작. 전환 시 GitHub Actions 자동 배포로 "드래그 업로드" 단계도 사라진다.
  통합 아키텍처 Phase 2 의 "검수 대시보드 v1" 시점이 자연스러운 후보.

## 7. 사람-LLM 판단 비교 뷰 (스펙 §5 선택 항목)

- `review_decisions` × `judge_runs` 조인으로 일치율·불일치 사례 테이블. 결정 데이터가
  어느 정도 쌓인 뒤(수십 건) 착수해야 의미 있음. ⛔ 성과 판정·승격에는 쓰지 않는다.
- ⚠ **선행 조건 — judge 노이즈 바닥 측정**(2026-08-06): 같은 클립 재실행 편차가
  0.025~0.375(중앙값 0.05, 재실행 9쌍 실측)다. 이 값을 모르면 "일치/불일치" 선을 그을 수
  없다(실제로 0.025 차이를 '역전'으로 읽을 뻔했다). 클립 5×3회면 분포가 나온다.
- 관련: `docs/JUDGE_GOLDEN_PAIRS.md` — 사람이 우열을 가렸는데 judge 가 변별 못 한 쌍.
  `review_decisions` 에는 **비교 판단을 담을 자리가 없어** 임시로 파일에 모은다.

## 12. judge 루브릭 v3 후보 — 서사 계약 축 (맥2 2026-08-06 제안)

- 문제: 루브릭(hook_3s·visual_hook·pacing·completion_pull)에 **정보 배치를 보는 축이 없다.**
  훅 두 축은 첫 3초 시각 임팩트만 재고, completion_pull 은 변별에 실패한 사례가 있다
  (golden 쌍 #1: 사람 2인이 가른 쌍을 judge 는 0.025 차 = 노이즈 이하로 구분 못 함).
- 제안: 훅 축을 쪼개는 대신 **세그먼트 관계**를 채점 — "훅이 만든 시청 동기를 build 가
  받는가, payoff 가 그 약속의 보상인가". 폼 중립적이라 reaction-first/event-first 어느 쪽도
  우대하지 않는다(훅 '궁금증' 축은 반대 편향을 새로 만든다).
- 지위: **표시 전용 v3 후보**. ⛔ 성과 판정·승격·자동 결정 금지(§7 유지).
- 결정 필요: 도입 여부 · 총점 반영 방식(단순평균 유지 시 새 축이 희석된다) ·
  judge 입력에 edit_plan 의 role·서술·내레이션을 넣을지.

## 13. story 구성 노브 후보 — hook_form (맥2 2026-08-06 제안)

- reaction-first(결과 선공개) vs event-first(시간순). golden 쌍 #1 에서 사람 선호가 처음
  잡혔지만 **관측→규칙 직행 금지(§7)** 라 노브화 → 쌍 A/B 가 정석 경로.
- K7(제목·해시태그) 동결에는 걸리지 않는다 — 훅은 영상 앞부분 자체라 apv·시청유지로 측정된다.
- 결정 필요: 노브 스펙 · 쌍 확보 계획(부호검정 최소 5쌍) · 소재 유형별 층화 여부.

## 8. 중복 판정에 의미 단위 추가 (맥2 2026-08-05 중복 실태 조사에서 제안)

- 문제: 중복 판정이 렌더된 시간 구간(IoU)만 비교해 "구간은 다른데 같은 공연·코너의
  다른 컷"을 못 거른다. 맥2 전수 조사에서 반려·발행분 간 구간 겹침 0초인데 의미상
  동일한 사례 다수(숏나우저 EP410 같은 무대 843-892s/790-840s, 제목도 판박이).
  8/5 낮 재생성 3편 전부 이 사유로 운영자 반려.
- 제안: 구간 IoU 외에 스토리 빔/제목 유사도 비교 추가. 스토리 후보 풀이 이미
  텍스트로 있어 비교 재료는 존재한다.
- 결정 필요: 도입 여부 · 유사도 기준(임계값) · 판정 위치(생성 전 후보 필터 vs 렌더 후).

## 9. 소재 소진 회차 자동 스킵 (맥2 2026-08-05 제안)

- 문제: 회차당 공개 3편 규칙 + 소스가 단일 코너 클립이면 소재 풀이 1~2편 만에
  소진되는데, 지금은 사람이 works.json start_episode 를 손조정해야 한다(8/5 세 채널
  실제 발생 — 놀토 410→411·산지직송 1→2·스트릿 1→2). 매번 사람이 정본을 고치는
  건 사고 위험. 반증: 숏테토칩은 회차 전진만으로 새 소재가 나와 합격·발행됨.
- 제안: 공개 3/3 미달이어도 재생성 N회 연속 반려 시 다음 회차로 자동 전진.
- 결정 필요: 도입 여부 · N 값 · 반려 사유 구분(중복 반려만 셀지) · 스킵 기록 방식.

## 10. 짧은 단일 코너 소스는 회차당 목표 하향 (맥2 2026-08-05 제안)

- 문제: 소스가 500~600초 단일 코너 클립인 작품(칼라페·스트릿 등)은 회차당 3편이
  구조적으로 무리 — 소재 4개 미만에서 3편을 뽑으면 재조합만 나온다.
- 제안: works.json 카드에 회차당 목표 편수 필드(예 `per_episode_target`)를 두고
  이런 작품은 3 미만으로. #9(자동 스킵)와 택일 또는 병행.
- 결정 필요: 도입 여부 · 필드 스펙 · 기존 카드 소급 값.

## 11. ⚠️ 사람 확인 대기 — 맥2 8/5 조사의 별도 발견 2건

- **도깨비 EP4 실제 중복 발행**: 같은 구간 [479-605]로 쇼츠 2편이 모두 발행돼 있음
  (`id4Hiv65wu0` / `d8ua6hu9P6o`, 7/26 수동 시딩분). 한 편 비공개 전환 여부 결정 필요.
- **스트릿 발행분 `QGsdCY_knI4` 오소스 — DB 대조로 확정(2026-08-05)**: 시딩 파일 섞임.
  7/24 시딩 3편이 모두 `Downloads/sources/street/source.mp4` 로 생성됐는데, 첫 실행
  (02:54, QGsdCY_knI4)만 스토리에 덱스·알리망오·타이거새우(산지직송 출연진/소재)이고
  이후 두 실행(04:49 hfS8-9bACqQ · 08:39 GcKdc_tVfk4)은 정상 스트릿 내용(에드워드 권·
  셰프·주문 폭주). 즉 시딩 시작 시점에 street 폴더에 잘못된 파일(산지직송 클립)이
  있었고 이후 올바른 파일로 교체됨. 같은 타이거새우 장면이 너굴안방 발행분
  `Ea334qKq6Xg` 에도 있어 두 채널에 같은 소재가 공개돼 있다. 원본 파일은 삭제되어
  영상 실물 대조는 불가. → 흥행수집 채널에 산지직송 콘텐츠가 스트릿 라벨로 발행된
  상태 — 비공개 전환 여부 운영자 결정 필요.

# 시크릿 (로컬 전용 — 절대 커밋 금지)
.env
.env.*
!.env.example

# python
__pycache__/
*.pyc
*.egg-info/
.venv/
venv/

# OS / 데이터
.DS_Store
outputs/
*.mp4
*.mkv

# scene_loop 머신 전용 — 배정·작품 속성·정책의 정본은 config/assignments.json ·
# works.json · loop_policy.json(전부 추적)이다. 여기 있는 것은 그 머신에서만 의미가 있다.
# ※ results/ 전체를 무시하면 추적 중인 리포트(results/knob_proposer_report.md 등)가 빠지므로
#   개별 지정한다.
config/scene_loop.json
config/scene_loop.local.json
results/scene_loop_state.json
results/scene_loop.log
results/scene_loop.launchd.*.log
results/scene_loop.lock/
results/youtube_index/

# 실행하면 다시 만들어지는 산출물 (2026-07-28 추적 해제)
factory/report.html
factory/ip_identify_test.json

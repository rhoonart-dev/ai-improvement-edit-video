# 키 이름만 문서화 (값은 커밋 금지 — 쉘 export 또는 비밀 저장소 사용).
GEMINI_API_KEY=
# Supabase 대시보드 → 프로젝트 → Settings → Database → Connection string → URI
LAEEBLY_DB_URL=
PIPELINE_DB_URL=
AI_VIDEO_ROOT=/Users/gimsewon/rhoonart/ai-video
